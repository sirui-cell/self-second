# self-second
sortTargets.py:根据胜率对wallet进行排序并去重，同时生成一个新文件，内容为可以直接打开的url
config.py:一些定义的变量，比如需要访问的url、token
fileHandle.py：对文件进行读写处理，和通过tg发消息
getData.py:根据config里面的url请求需要处理的数据，url需要定时更新，因为api会变

good luck
